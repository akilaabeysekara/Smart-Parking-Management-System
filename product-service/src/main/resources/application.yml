spring:
  application:
    name: product-service

server:
  port: 8081
  servlet:
    context-path: /product-service

# context-path: /product-service included every request
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    prefer-ip-address: true
